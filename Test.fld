@comment test

hello = "hello" @ Process Global
myTable = [hello, "object 1"] @array index will start at 0

dictionary = {
  Dick = "";
}

print(myTable[1]) @ "object 1"

while (1 == 1) {
    break
}

@i am a comment

if (true) {
    hello()
}

function hello(para1, para2) {
    return hello + " world" @hello world
}

for Index, Value in myTable { -- Key map for both objects and arrays

}
